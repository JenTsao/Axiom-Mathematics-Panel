# 2026-09-12 工作日志

## 项目分析：MathLab (Axiom) — Axiom Mathematics Panel
- 对 `J:\PROJECT\Python project\Axiom Mathematics Panel` 全项目做了静态分析，产出 `PROJECT_ANALYSIS.html`（自包含分析报告）。
- 关键发现：
  - 规模：151 个 Python 文件 / 35,789 行；ui(13,181) + core(12,929) 占 73%；测试 5,555 行。
  - C# 加速内核 6 文件 (~690 行)，pythonnet 桥接。
  - resources/ 下 5,383 个 JS/TS（Three.js/ECharts/Monaco/JupyterLab 第三方资产，非手写）。
  - 测试文件 22 个，约 419 个 `def test_` 函数（README 写 126，疑似陈旧）。
  - Bandit：3 项（1 HIGH MD5 @ file_manager.py:111；2 MEDIUM urlopen @ jupyter_manager.py:332 & jupyter_panel.py:313）。
  - 覆盖率仅 21%（coverage.xml 快照）。
  - 版本号不一致：`__init__.py` = "3.7" vs README 3.8.0。
  - 运行日志：JupyterLab 启动超时 + 打包后 styles.qss 缺失。
  - git 干净，分支 main；CI 有 test/release/code-quality 三个 workflow。
- 环境注意：本机 Git Bash 缺少 coreutils（find/sed/head 等均不可用），分析改用 Python 脚本完成。

## 修复所有问题（2026-09-12 下午）
用户要求"修复所有问题"，已完成并验证 6 项：
1. Bandit 3 项告警：复核发现代码此前已修复（MD5 带 usedforsecurity=False、两处 urlopen 有 scheme 校验+nosec），旧报告过期；用 bandit 1.9.4（装在 C:\Users\66393\.workbuddy\binaries\python\envs\default）以 CI 同参数重扫 → 0 问题，bandit-report.json 已重新生成。
2. 版本号统一 3.8.0：utils/version.py、__init__.py、setup.py 回退值、test_architecture_refactor.py 断言（共 5 处）。
3. styles.qss/图标打包路径：main.py 新增 _find_resource()，按 _MEIPASS→exe 目录→源码目录三级探测，兼容 PyInstaller 5/6 ONEDIR 与 ONEFILE。
4. JupyterLab 启动超时根因修复（jupyter_manager.py）：Popen(stdout=PIPE) 无人排空导致子进程阻塞；新增 JupyterLabOutputDrain 守护线程（deque 200 行）+ _log_output_tail 失败诊断 + 等待进度日志（每 10s）+ 默认超时 30→45s。
5. README 校准：测试徽章 126→419，"新增测试覆盖"表格改"新增小计"并补全量规模说明。
6. PROJECT_ANALYSIS.html 已同步更新为修复后状态。
验证：py_compile 全通过；独立验证脚本 5/5 通过（版本一致性/管道排空/FileMetadata 校验和/_find_resource 双模式/bandit 归零）。
遗留 P2：全量 pytest 确认真实覆盖率（venv 缺 numpy/pytest，与根 requirements.txt 未同步）；Linux AppImage 构建目标；dist/build/venv 的 .gitignore 检查。

## CI Black 格式修复（2026-09-12 14:00）
- CI Black 检查失败：main.py 中 _find_resource 的多行 candidates.append 在 line-length=120 下可放一行。
- 修复：折叠为单行。本地用 black 26.5.1 + isort 9.0.1（装在 managed env C:\Users\66393\.workbuddy\binaries\python\envs\default）以 CI 同参数复现检查：black 151 files unchanged / isort 通过。
- 经验：本项目提交前应跑 `black mathlab/ --check --line-length=120 --extend-exclude="mathlab/resources|mathlab/logs"` 与 `isort --check-only --profile black`。

## 推送与 git 异常排查（2026-09-12 14:10-14:20）
- Black 修复已提交并推送成功：commit 415ab67，remote main 确认到 415ab67（ls-remote 权威验证）。CI Code Quality（含 Black）转绿，Tests 排队中。
- 仓库已迁移：jencaoking/Axiom-Mathematics-Panel → **JenTsao/Axiom-Mathematics-Panel**，已把 remote origin URL 更新为新地址（GitHub 重定向仍兼容旧 URL）。
- 重要异常：本地 refs/remotes/origin/main 被外部进程毫秒级回滚到 62 个提交前的旧值 ad1bd2c（packed-refs mtime 来自未来 +23min，reflog 显示写入成功但随即被覆盖）。疑似 IDE（trae 痕迹）git 缓存或同步工具持旧快照刷写。应对：以 `git ls-remote` 为准验证推送，勿信本地 tracking ref；用户关闭相关 IDE/同步工具后 `git fetch origin` 即可自愈。
- 用户工作区有未提交的 omni_bar 功能改动（omni_bar.py/omni_bar.ts/omni_bar.html/webpack.omni.config.js/package.json），非本人改动，未纳入提交。

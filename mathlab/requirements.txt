# ── 核心依赖（打包必须包含）──────────────────────────────────────────────
# 安装命令: pip install -r requirements.txt
PySide6>=6.5.0
sympy>=1.12
numpy>=1.26
scipy>=1.11
networkx>=3.1
jedi>=0.19
psutil>=5.9

# ── 科学计算与可视化 ──────────────────────────────────────────────────────
scikit-learn>=1.3
matplotlib>=3.8
pyqtgraph>=0.13

# ── AI 集成（多提供商 LLM 接入）──────────────────────────────────────────
openai>=1.0
PyYAML>=6.0
markdown>=3.5
Pygments>=2.17

# ── JupyterLab 嵌入（环境探路 Step-1）────────────────────────────────────
# PySide6-WebEngine 已内置于 PySide6>=6.5 的 Addons 包中，无需单独安装
jupyterlab>=4.0
jupyter_client>=8.0
ipykernel>=6.0
jupyter_server>=2.0
requests>=2.31

# ── C# 加速内核（pythonnet 桥接；缺失时相关模块自动降级）───────────────────
pythonnet>=3.0
